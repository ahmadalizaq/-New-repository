# FEEL — دليل التشغيل والنشر (مجاني بالكامل)

هاد التطبيق أونلاين حقيقي: تسجيل حسابات، قاعدة بيانات مشتركة بين كل الزوار،
صفحة أدمن، ونظام حظر. الباكند وقاعدة البيانات على **Supabase** (مجاني)،
والاستضافة على **Vercel** أو **Netlify** (مجاني).

---

## الخطوة 1 — أنشئ مشروع Supabase

1. روح لـ [supabase.com](https://supabase.com) وسجّل حساب مجاني.
2. اضغط **New Project**. اختار اسم، باسورد لقاعدة البيانات (احفظه بمكان آمن)، والمنطقة الأقرب إلك.
3. استنى دقيقة لين يخلص إنشاء المشروع.

## الخطوة 2 — شغّل قاعدة البيانات

1. من القائمة الجانبية، افتح **SQL Editor**.
2. اضغط **New query**.
3. افتح ملف `schema.sql` المرفق، انسخ **كل محتواه**، والصقه بالمحرر.
4. اضغط **Run**. المفروض يشتغل بدون أخطاء وينشئ كل الجداول والصلاحيات تلقائيًا.

## الخطوة 3 — عبّي بيانات الاتصال بالموقع

1. من القائمة الجانبية بـ Supabase، افتح **Project Settings > API**.
2. انسخ **Project URL** و **anon public key**.
3. افتح ملف `supabase-client.js` بمشروعك، واستبدل:
   ```js
   const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
   const SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
   ```
   بالقيم يلي نسختها.

هاد المفتاحين آمن يكونوا بالكود الظاهر للمستخدم (Supabase مصمم هيك) —
الحماية الفعلية موجودة بسياسات RLS يلي بملف `schema.sql`.

## الخطوة 4 — جرّب محليًا (اختياري بس مفيد)

افتح `index.html` مباشرة بالمتصفح، أو الأفضل شغّل سيرفر بسيط بمجلد المشروع:

```bash
npx serve .
```

جرّب تسجّل حساب جديد وتأكد كل شي شغال قبل ما تنشر.

> ملاحظة: إذا فعّلت "Confirm email" بإعدادات Supabase Auth (مفعّل افتراضيًا)،
> المستخدم لازم يفتح إيميله ويأكد الحساب قبل ما يقدر يسجّل دخول. تقدر تعطّل
> هاد الخيار من **Authentication > Providers > Email** لو بدك التسجيل يصير فوري
> بدون تأكيد (أسهل للتجربة، بس أقل أمان للإنتاج الحقيقي).

## الخطوة 5 — خلّي حسابك أدمن

1. سجّل حساب عادي من صفحة "حساب جديد" بالموقع.
2. رجع لـ Supabase SQL Editor ونفّذ (غيّر الإيميل لإيميلك الفعلي):
   ```sql
   update public.profiles set is_admin = true
   where id = (select id from auth.users where email = 'your-email@example.com');
   ```
3. سجّل خروج ودخول من جديد بالموقع — رح يظهر رابط "لوحة الأدمن" بالنافبار.

## الخطوة 6 — انشر الموقع مجانًا (Vercel)

1. ارفع مجلد المشروع (كل الملفات) على GitHub (repo جديد).
2. روح لـ [vercel.com](https://vercel.com) وسجّل دخول بحساب GitHub.
3. اضغط **Add New Project**، اختار الـ repo يلي رفعته.
4. بما إنه موقع Static (بدون build step)، اترك الإعدادات الافتراضية واضغط **Deploy**.
5. بعد دقيقة رح ياخدك لينك مباشر (مثلا `feel-app.vercel.app`) — هاد رابط موقعك الشغال أونلاين.

**بديل: Netlify** — نفس الفكرة تمامًا: اربط GitHub repo من [netlify.com](https://netlify.com)
واضغط Deploy، بدون أي إعدادات build إضافية.

---

## أشياء مهمة قبل ما تشارك الرابط مع ناس

- **الحظر (Ban):** أي حساب يصير عليه بان من صفحة الأدمن، ما يقدر يسجّل دخول
  وبيشوف رسالة توضح السبب.
- **كلمات المرور:** ما تنخزن ولا تنعرض بشكل واضح لحدا، حتى الأدمن — هاد
  قياسي بكل تطبيق حقيقي.
- **حدود Supabase المجانية:** الخطة المجانية تكفي مشروع صغير/متوسط (500MB
  قاعدة بيانات، 50,000 مستخدم شهريًا تقريبًا). إذا الموقع كبر كتير بتحتاج
  ترقّي لخطة مدفوعة لاحقًا.
- **تعديل التصميم:** ألوان المشاعر والنصوص كلها بملف `config.js` —
  تقدر تعدّل عليها براحتك بدون ما تلمس باقي الكود.
